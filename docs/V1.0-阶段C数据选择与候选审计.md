# Agent Memory — 阶段 C 数据选择与候选审计

> 审计日期：2026-07-19；最新结论：**已按人工语义修正重建 3 个金标草案，等待最终接受**。

## 1. 数据选择审计

本次只读取本地脱敏 Hermes 导出，不调用模型、不发送外部数据、不写数据库，也不修改生产
Hermes。来源文件 SHA-256 为
`a04db412fefbd85a470d9d7622375900558c73ea91d3b8ddb7f30b0519667a05`。

| 范围 | Session | 自动任务 | 交互会话 | 结论 |
| --- | ---: | ---: | ---: | --- |
| 完整脱敏导出 | 84 | 79 | 5 | 自动任务占比过高 |
| 旧 RC2 固定选择 | 30 | 28 | 2 | 不适合社区校准 |
| 阶段 C 交互选择 | 5 | 0 | 5 | 已用于本轮候选审计 |

旧选择计划 SHA-256 为
`770a482a1d06f714417fcd2cf3dd743a74b068fb7f6a56944f8e536e42d97792`；新选择计划
SHA-256 为 `880a06d43ec8fea0212a4b1d099ca0cd292172b5be70916bf10035dc122e2cde`。
新计划使用 `--exclude-automated` 确定性生成，并在候选扫描阶段再次防御性排除自动任务。

## 2. 保守候选结果

候选扫描仅保留 user 直接证据中的可读、唯一实体和受控直接观察工具结果；assistant 内容只用于统计
重复出现，代码符号、告警字段名、SmokeTest 事件名、内部 ID 和状态词均不作为行星。修正规范化后，
`Hermes Studio` 与 `Hermes WebUI` 合并为 `Hermes WebUI / Studio`，`Agent Bridge` 作为内部组件排除。
近期交互结果为：

| 候选关系 | 独立消息 | Session | 结构判断 |
| --- | ---: | ---: | --- |
| Home Assistant — Xiaomi 智能音箱 Pro | 6 | 1 | 只有 2 个行星 |
| Alertmanager — hermes-alert-relay | 3 | 3 | 只有 2 个行星 |

共得到 7 个可读实体、2 条候选关系和 2 个二节点连通分量。达到“至少 3 个唯一行星、2 条边、
2 个独立事实”结构门槛的社区为 **0**。这些内容仍是 `REVIEW_REQUIRED` 候选，不会自动写入记忆或
社区表。聚合结果见
[`V1.0-阶段C前置候选覆盖摘要.md`](V1.0-阶段C前置候选覆盖摘要.md)；含脱敏摘录的私有审核文件
只保存在被 Git 忽略且权限为 0600 的 `data/reviews/`。

## 3. 为什么现在不能继续算法选型

当前这份近期数据只能证明两组真实的二元关系，无法形成三个可用于比较 Louvain、Leiden 或其他社区算法的
人工金标社区。如果把 receiver、alertname、状态词、代码类名或 assistant 推测补成第三颗行星，
虽然统计会“达标”，但会重新引入此前已经发现的不可读伪实体，并违反禁止推理生成虚假事实的
边界。

因此阶段 C 的正确阻塞点是代表性交互数据覆盖，不是算法或 UI。现有自动社区最低门槛保持不变。

## 4. 解除前置阻塞所需数据

优先方案是补充一份本地脱敏、排除 cron/自动通知注入的 Hermes 交互会话导出：

- 建议 20–50 个真实交互 session；
- 至少覆盖 3 个自然主题，例如家庭设备、监控服务、项目开发/排障；
- 每个主题应自然出现至少 3 个唯一实体；
- 每个候选关系至少有 2 条独立 user 消息、工具观察或可追溯事实支撑；
- 不需要为了满足数量人工编造对话，也不需要提供 Vault、密码、密钥或地址等敏感明文。

拿到导出后，仍先在本地生成候选审核清单和覆盖报告，达到金标条件后再进入社区算法比较与阶段 C
持久化设计。

## 5. 历史备份补充审计

用户授权只读使用
`/Users/evergarden/Documents/hermes-session-backups/qishuo-weekly-20260718-191952`。本轮没有读取
正在运行的生产 Hermes，也没有修改备份目录。

- 备份 manifest 中 6 个文件的 SHA-256 与实文件全部一致；
- 两个 SQLite 快照均以 `immutable=1` 执行 `PRAGMA quick_check`，结果为 `ok`；
- 使用精确的七天前删除候选快照，共 104 个 session、13,337 条消息；
- 规范化后排除 system prompt 和 reasoning，执行本地固定点脱敏，共处理 1,938 个敏感命中；
- 识别并排除 9 个 cron session，剩余 95 个非自动 session；
- 以 50 + 45 两个互斥固定选择批次覆盖全部 95 个 session，没有重复或漏选；
- 不调用模型、不发送外部数据、不写数据库，规范化文件、选择计划和含摘录审核文件均位于
  Git 忽略目录且权限为 0600。

候选复核继续排除压缩摘要、提示包装、伪装成 user 的 JSON 工具包装、非直接环境观察工具结果、代码
标识和无关系谓词的共同出现；多实体句只从具备显式关系谓词的二元子句提边。历史备份、近期交互
合并后先产生的草案包含错误规范实体和伪路径边，已根据用户确认的运行时与基础设施语义重建为 3 个
结构通过草案，见
[`V1.0-阶段C人工金标社区草案.md`](V1.0-阶段C人工金标社区草案.md)：

1. PostgreSQL 记忆后端；
2. Loki 日志告警链路；
3. Hermes 邮件工作流。

其中 PostgreSQL 是数据库社区 core，Hindsight/Honcho 是使用方 satellite；Loki 是告警链路 core，
Alloy 与 Alertmanager 分别承担日志输入和告警输出 bridge；Himalaya 是 Agent 与 Outlook 之间的
bridge。`core/bridge/satellite` 均为社区内角色，不是全局实体类型。

旧 Hermes 运行时草案被撤销：WebUI 与 Studio 是同一规范实体，Agent Bridge 默认只是内部组件，
因此只剩 `Hermes Agent — Hermes WebUI / Studio` 二元关系，不强制形成星系。旧
`PostgreSQL — Tailscale` 边也被撤销：应用—数据库依赖继续保留，局域网直连记录为边属性；只有
实际路由或已配置回退路径有证据时，Tailscale 才作为 bridge。Portainer 仅表达管理平面关系，不因
容器清单共同出现成为数据库 satellite。

三个草案当前均为 `REVIEW_REQUIRED`。数据覆盖阻塞已经解除；阶段 C 的剩余前置条件是用户对名称、
成员和关系边执行 `ACCEPT`、`CORRECT` 或 `REJECT`，不得以结构通过替代人工金标确认。

可提交、无对话摘录的草案配置保存在 `config/phase-c-gold-drafts.json`；含脱敏证据摘录的候选审核
文件仍只保存在被 Git 忽略的 `data/reviews/`。复跑校验不会调用模型、发送外部数据或写数据库。
