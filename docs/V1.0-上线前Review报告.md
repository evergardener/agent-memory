# Agent Memory V1.0 上线前 Review 报告

> Review 日期：2026-07-20。候选源码：`1.0.0-rc.7`。范围仅限本仓库和全新隔离发布环境，未修改生产 Hermes、正式 namespace、在线数据库或现有 Provider 配置。

## 1. 结论

代码与隔离发布 Gate 达到 **GO**；正式部署目前仍为 **NO-GO**。阻断项不是已知代码故障，而是上线授权和生产管理面尚未闭环：根目录 `.env` 未恢复、尚未基于上线时点创建生产备份、正式关系计划尚未执行、单 profile 灰度尚未获准。

`rc.7` 可以作为待签署候选继续打包和灰度准备，不能据此自动重启当前标准容器或修改生产 Hermes。

## 2. Review 中发现并修复的问题

| 等级 | 问题 | 修复与验证 |
| --- | --- | --- |
| P0 | release-check 会复用标准镜像名、固定数据目录与网段，存在干扰在线栈风险 | 发布环境强制独立 project、镜像前缀、数据/备份/Vault 路径、端口和网段；预检拒绝正式 namespace、主端口、主网段、非空数据目录和外部模型 |
| P0 | 已审关系写入器只能写影子空间，缺少正式提升的失败关闭路径 | 增加计划 SHA、正式 namespace 原文确认、固定确认短语、change ID、备份清单 SHA 和逐文件校验；默认仍拒绝正式写入 |
| P1 | 审计记录在同一事务内时间戳相同，撤销可能按随机 UUID 选错操作 | 新增 `0014_audit_event_order` 单调事件序号，撤销按数据库序号逆序；数据库与 HTTP 回归通过 |
| P1 | 阶段 C 数据库/API 测试未标记为 integration，发布脚本会静默跳过 | 补齐 marker、独立关系夹具和显式数据库回归，18 项隔离集成测试与额外 3 项社区数据库测试通过 |
| P1 | UI 登录会话可调用原始 turn ingest | ingest 收紧为 service token 专用；UI cookie 注入证据的回归测试通过 |
| P1 | Hermes Provider 可配置远程 API 地址，偏离本地安全边界 | 客户端只接受无凭据、无额外路径的 loopback HTTP origin；LAN、远程主机和 HTTPS 均失败关闭 |
| P1 | 候选镜像可能覆盖标准标签，且镜像缺少源码溯源 | 发布镜像使用隔离前缀；正式 Gate 要求干净 Git 工作树，镜像写入并核对 OCI version/revision 标签 |
| P2 | `/v1/graph` 仍返回 `phase-c-pending` | 统一为 `community-projection-v1` 并补回归 |
| P2 | Docker Desktop 对仅连接 internal 网络的 PostgreSQL 不建立验收端口 | 只在 `compose.release.yaml` 临时加入 edge，数据库仍只发布到 `127.0.0.1`；正式 Compose 不发布数据库 |
| P2 | TypeScript build mode 复用 `noEmit` 增量状态时可能长期挂起 | 前端 Gate 改为无增量的显式 app/node typecheck，再执行 Vite build；连续 typecheck/build 通过 |

## 3. 安全与运行审查

- API/worker/model-worker 根文件系统只读，`cap_drop=ALL`，启用 `no-new-privileges`；
- API 仅发布到 `127.0.0.1`；core worker 仅连接 internal backend；model-worker 单独拥有 edge；
- release 环境模型默认关闭，不含模型 key，不允许发送外部数据；
- release env 与 Vault key 权限要求为 `0600`/`0400`，测试 UI 密码仅初始化时显示一次；
- API readiness 使用数据库就绪检查，不再以进程存活代替可服务；
- npm 生产/开发依赖审计均为 0 个已知漏洞；锁定的 Python 生产依赖审计无已知漏洞；
- 仓库密钥扫描未发现真实凭据，只有脱敏器测试使用的伪 key；
- 日志无阻断错误。PostgreSQL 初始化时的 local `trust` 提示来自官方镜像本地 socket 初始化流程；正式网络访问仍使用密码且数据库不发布宿主端口。

## 4. 最终隔离 Gate 证据

全新 `agent-memory-release-review-v4` 环境使用独立数据根、独立备份根、独立 Vault key、`172.16.248.0/24` 与 `172.16.249.0/24`，未复用在线项目资产。结果：

- Ruff、完整 pytest、前端 typecheck/build：PASS；普通 pytest 中 18 项外部集成测试按设计跳过；
- 隔离 API/图治理/Hermes 回归：18 项 PASS；Hermes live Provider：4 项 PASS；
- 原子抽取自检、派生记忆脱敏自检：PASS；
- 社区投影数据库测试：3 项 PASS；
- Worker outage recovery 与 lease recovery：PASS；
- 备份 `SHA256SUMS`、空库 pg_dump restore、计数比较和 Vault 实际解密：PASS；
- ARM64 候选镜像、readiness、运行日志和安全属性检查：PASS。

最终正式 Gate 需在本报告对应提交完成后再次运行，以便 OCI revision 与干净 Git HEAD 完全一致。

## 5. 正式上线前剩余 P0

1. 从匹配在线数据库和 Vault key 的已校验备份恢复根目录 `.env` 管理面，只比较配置指纹，不输出秘密；
2. 基于维护窗口开始前的正式数据执行 `backup.sh` 与 `verify-restore.sh`，将 Vault key 另介质保存；
3. 对生产备份副本预演 `0013 -> 0014`，比较 evidence 哈希、核心计数、审计序号和旧 API 兼容性；
4. 生成正式关系 preview，人工核对计划后使用计划 SHA、备份清单 SHA 和 change ID 授权；
5. 获得三个独立授权：正式 namespace 迁移、`rc.7` 发布、一个非关键 Hermes profile 灰度；
6. 完成 2/24/72 小时灰度观察后，才允许扩大 profile 范围。

## 6. 当前运行资产特别说明

标准 `7788/7790` 与阶段 C `7796/7797/7798` 容器仍运行旧快照，本次 Review 未重启它们。标准 `rc.6` 标签为防止被先前隔离构建误用而保持缺失；在生成、验证并明确批准 `rc.7` 前，不得对标准栈执行 Compose 重建。
