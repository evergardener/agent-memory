# Agent Memory V1.0 H0–H6 治理实施验证报告

- 日期：2026-08-07
- 候选版本：`1.0.0-rc.9`
- 实施环境：仅 `127.0.0.1` 隔离 API/worker/frontend 与专用隔离数据库
- 生产状态：未迁移、未重放、未部署；仅在恢复验证备份副本完成 H5 只读 dry-run

## 1. 实施结果

| 阶段 | 已完成源码 | 验证结果 | 仍需独立授权 |
| --- | --- | --- | --- |
| H0 | purge 依赖保护、限定模型任务 dry-run/requeue、运行/质量健康拆分、失败与脱敏计数 | PASS | 生产失败任务重放 |
| H1 | current 字段迁移、唯一 transition service、fact/index 同步、默认过滤、历史 trace | PASS | 生产历史状态 dry-run/apply |
| H2 | 未完成+近期/跨会话 Gate、瞬时状态 evidence-only、模型不可绕过规则 | PASS | 生产 shadow 对比 |
| H3 | 称呼/语言/回复风格/操作约束/提醒方式解析、多 evidence、冲突 supersedes、USER memory 只读预览 | PASS | 生产偏好样本抽检 |
| H4 | query alias、低信息过滤、通道分数、min score/margin、no-match、ADR | PASS | 真实 embedding/rerank 新数据授权 |
| H5 | manifest SHA、默认 dry-run、显式确认 apply、证据/Vault 完整性硬检查、历史 candidate 四态只读命令 | PASS（生产备份副本） | 生产数据 apply 授权 |
| H6 | rc.9 版本、现有 GHCR/SBOM/attestation 契约、隔离自动化/浏览器/恢复 Gate | PASS（隔离） | 推送、镜像构建、生产部署、72 小时观察与晋级 |

## 2. 数据库与自动化证据

- Alembic：空隔离数据库 `upgrade head` → `downgrade 0016_unified_memory` → `upgrade head`，最终
  `0018_preference_evidence (head)`。
- 单元/静态：Ruff PASS；当前工作树 `226 passed, 33 skipped`（集成项在隔离环境单独运行）。
- F2–F7 与治理完整集成：`32 passed`。
- 数据库治理集成：7 组场景全部 PASS：
  - current resolve 同步 item/fact/document，重复 resolve 不增加版本；
  - 偏好重复证据复用记录，相反/替代值只保留一个 active；
  - purge 遇到 preference evidence 等派生引用不再删除仍被使用的 evidence；
  - 模型重放和历史治理默认 dry-run，错误确认或 manifest 漂移失败关闭。
  - 五类显式偏好完成 ingest → fact → unified preference → evidence 全链路；
  - H4 正向、负向和 resolved 召回金标达到计划门槛。
  - 历史 candidate 预过滤、模型四态校验、调用预算和报告正文隔离均为只读且失败关闭。
- H3 确定性偏好金标：称呼、语言、表达风格、操作约束、提醒方式以及负例共 48 项，
  `48/48`；完整 ingest → fact → unified preference → evidence DB 回归 PASS。
- H4 隔离 PostgreSQL 金标：10 个自然改写 `10/10` top-1，100 个 UUID/哈希/自然负例
  `0/100` 误命中，resolve 后 10 个正向问法 `10/10` no-match；本地哈希仍只作低权重 fuzzy，
  中文邮件别名由受控 lexical alias 通道处理。
- 前端：6 个星图模型用例、TypeScript、Vite build 全部 PASS。

## 3. 浏览器与故障恢复

- 浏览器完成隔离登录、星图、当前状态和整理报告页面检查。
- 页面可显示主体/实体/关系、Current State、治理队列；浏览器 console 无 warning/error。
- worker 停止期间合成证据对应任务保持 `pending`；worker 重启后同一任务转为 `done`。
- 完整 integration 首轮暴露 interaction simulate 只读断言与后台 worker 快照更新竞态；测试改为
  先等待状态稳定再验证 simulate 不写入，失败的 3 项定向重跑 PASS，随后完整 `32/32` PASS。
- 测试结束后 API、worker、前端和专用测试数据库/角色均已清理；隔离 PostgreSQL 的
  `template1` 仅补装测试所需的 pgvector 扩展，原隔离业务数据库和全部生产容器/namespace 未修改。

## 4. 发布边界

源码完成不等于生产授权。下一步只能先提交 `rc.9` 源码；远端推送会触发公开 GHCR 工作流，
需用户明确允许将本次治理源码和文档推送到 `origin`。即使镜像构建完成，也必须另行取得：

1. 生产只读 dry-run 授权；
2. H5 数据 apply 授权；
3. H6 生产部署授权。

三项不可合并推定，且生产状态在 72 小时和用户主观验收前继续保持 `canary_active`。

## 5. 最新审计闭合结果

2026-08-07 已补跑 H3 完整写入、H0–H5 治理、完整 integration 和 worker outage/recovery Gate；
临时数据库、角色、API、worker 与 Vault key 均已清理。H0–H6 的源码与隔离验证已闭合，但这不
等于生产授权；远端构建、H5 生产数据 apply、H6 生产部署和观察窗口仍按独立边界执行。

## 6. H5 生产备份副本补充证据

2026-08-07 使用新鲜备份 `20260807T145106Z` 恢复到仅绑定 `127.0.0.1:55438` 的专用数据库，
从 `0016_unified_memory` 升级至 `0018_preference_evidence` 后核对为 12,217 events、107 facts、
38 candidates。四态 dry-run 得到 `discard=22`、`evidence_only=16`、`accept=0`、`review=0`；
模型调用 0、外部发送 false、数据库写入 0。19 条 candidate 与 active 事实完全重复，另有 3 条
candidate 内部冗余，剩余 16 条由不可召回门禁处理。

私有报告位于生产治理报告目录，权限 `0600`，不含 statement/content/prompt/response 字段；输入
快照 SHA 为 `3f5fcd28c86283e3ab1f4bdc78e8fb7666b09953010597cd17194aa4a8dc9808`，manifest SHA 为
`c6bc9a1f54255fca457c970003a120d958169e4e565a015b7a57b08bfc7fce79`。本阶段没有 apply 能力，
也没有获得生产写入、推送、镜像或部署授权。
