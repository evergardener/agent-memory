# Agent Memory — 阶段 C 人工金标社区草案

> 生成时间：`2026-07-19T13:43:25+00:00`；版本：`phase-c-gold-review-v2`。

## 1. 状态

- 数据集：3。
- 结构通过：3 / 3。
- 人工接受：0 / 3。
- 当前状态：`REVIEW_REQUIRED`。
- 模型调用：否；外部数据发送：否；数据库写入：否。

## 2. 数据边界

| 数据集 | Session | 实体 | 关系 | 来源 SHA 前缀 | 选择 SHA 前缀 |
| --- | ---: | ---: | ---: | --- | --- |
| 历史备份-A | 50 | 31 | 14 | `5e3174e2c9fb` | `6fc5539cd0ac` |
| 历史备份-B | 45 | 2 | 1 | `5e3174e2c9fb` | `d2d4ef2258bd` |
| 近期交互 | 5 | 7 | 2 | `a04db412fefb` | `880a06d43ec8` |

## 3. 草案

### 1. PostgreSQL 记忆后端 (`postgresql-memory-backends`)

- 成员：PostgreSQL（core）, Hindsight（satellite）, Honcho（satellite）。
- 独立证据引用：24。
- 结构门槛：通过。
- 人工决策：`REVIEW_REQUIRED`。

| 关系 | 语义 | 传输路径 | 证据 | Session 合计 | 数据集 | 状态 | 备注 |
| --- | --- | --- | ---: | ---: | --- | --- | --- |
| Hindsight — PostgreSQL | uses_database | lan_direct | 17 | 5 | 历史备份-A | FOUND | 数据库依赖是持久语义；局域网直连只作为当前传输路径。 |
| Honcho — PostgreSQL | uses_database | lan_direct | 7 | 3 | 历史备份-A | FOUND | 应用是卫星；PostgreSQL 是该社区的核心。 |

### 2. Loki 日志告警链路 (`loki-alerting-pipeline`)

- 成员：Loki（core）, Alloy（bridge）, Alertmanager（bridge）。
- 独立证据引用：9。
- 结构门槛：通过。
- 人工决策：`REVIEW_REQUIRED`。

| 关系 | 语义 | 传输路径 | 证据 | Session 合计 | 数据集 | 状态 | 备注 |
| --- | --- | --- | ---: | ---: | --- | --- | --- |
| Alloy — Loki | pushes_logs_to | http_push | 7 | 2 | 历史备份-A | FOUND | Alloy 是日志采集与转发桥，Loki 是日志汇聚核心。 |
| Loki — Alertmanager | sends_alerts_to | http_api | 2 | 1 | 历史备份-A | FOUND | Loki ruler 通过已配置端点把告警发送给 Alertmanager。 |

### 3. Hermes 邮件工作流 (`hermes-email-workflow`)

- 成员：Hermes Agent（core）, Himalaya（bridge）, Outlook（satellite）。
- 独立证据引用：67。
- 结构门槛：通过。
- 人工决策：`REVIEW_REQUIRED`。

| 关系 | 语义 | 传输路径 | 证据 | Session 合计 | 数据集 | 状态 | 备注 |
| --- | --- | --- | ---: | ---: | --- | --- | --- |
| Hermes Agent — Himalaya | uses_email_connector | local_cli | 23 | 5 | 历史备份-A | FOUND | Himalaya 连接 Agent 能力与邮件提供方，因此是桥接行星。 |
| Himalaya — Outlook | connects_mailbox | oauth2_imap_smtp | 44 | 2 | 历史备份-A | FOUND | 认证和邮件协议是边属性，不另建行星。 |

## 4. 不形成星系的已确认边界

### Hermes 运行时关系

- 规范成员：Hermes Agent, Hermes WebUI / Studio。
- 处理：保留为强关系或临时星座，不生成自动星系。
- 原因：WebUI 与 Studio 已合并为一个规范实体；Agent Bridge 是内部组件而不是行星，规范化后只有两个行星。

### PostgreSQL 网络路径

- 规范成员：PostgreSQL, Tailscale。
- 处理：Tailscale 仅在实际流量路径或已配置回退路径有证据时作为 bridge；LAN 直连记录为边属性。
- 原因：业务依赖、传输路径和社区归属必须分开，资产清单共同出现不能证明数据库流量经过 Tailscale。

### 基础设施管理平面

- 规范成员：Portainer。
- 处理：仅保留对容器/部署对象的 management_plane 关系。
- 原因：Portainer 与服务共同出现在容器清单中，不等于它是数据库使用方。

## 5. 人工确认边界

结构通过只证明成员和关系均可回到候选证据，不证明社区命名或边界正确。在用户把每项决策改为 `ACCEPT` 前，草案不得写入 galaxy/layout 表，也不得作为召回事实。
